<h1>ID: <%= @image.id %></h1>
<%= image_tag @image.url %>
<h3>
  Tags:
  <% @image.tag_list.each do |tag| %>
    <a class='btn btn-light' href='<%= tags_path(tag) %>'><%= tag %></a>
  <% end %>
</h3>
