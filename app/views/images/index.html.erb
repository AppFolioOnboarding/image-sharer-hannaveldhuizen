<h1>Images</h1>

<table>
  <% @images.each do |image| %>
    <tr>
      <td><%= image.id %></td>
    </tr>
    <tr>
      <td>
        <%= image_tag image.url %>
        <h3>
          Tags:
          <% image.tag_list.each do |tag| %>
            <label class='btn btn-light'><%= tag %></label>
          <% end %>
        </h3>
      </td>
    </tr>
  <% end %>
</table>
